%h4 Form

= form_for card do |f|
  = f.label :title, "Title"
  = f.text_field :title
  = f.fields_for :words do |builder|
    .form-group
      = builder.label :value, "Word"
      = builder.text_field :value
  = f.submit 